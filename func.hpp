#pragma once

#include <iostream>
#include <stdio.h>
#include <string>
#include "train.hpp"

using namespace std;

void copy(char* a, char* b);

long int dlina(char* a);

bool chislo(char p);

bool test_number(char* str);

bool test(char* str);


